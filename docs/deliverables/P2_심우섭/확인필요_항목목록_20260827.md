# 미해결 / 확인 필요 항목

작성자: P2 심우섭 | 작성일: 2026-08-28

1. **`student_number` 필드가 실제로는 대부분 비어있음** — 해결됨
   - 학생 매칭 기준을 `student_number` 대신 `accounts_user.id`로 확정하여 이 문제는 더 이상 진행을 막지 않음
   - 화면 표시는 id 대신 `first_name`/`last_name`으로 처리하기로 함
   - 향후 학번 기준 기능이 필요해지면 그때 재검토
   - 우선순위: 낮음

2. **User-Student 통합 구조 확장성** — 현재 프로젝트 기준 해결 / 향후 확장 시 재검토
   - 현재는 User 테이블 안에 학생 정보가 통합되어 있으며, 이번 프로젝트 범위 내에서는 이 구조로 충분함
   - 향후 Student 전용 정보(소속 학년/기수 등)가 늘어나는 시점에 테이블 분리 여부를 재검토
   - 우선순위: 낮음 (현재는 이슈 아님)

3. **ERD상 TBD 항목**
   - 과제모듈 중복 등 일부 항목이 아직 미해결 (3조 관련 결정 시 갱신 예정)
   - 우선순위: 중간

4. **3조 Core User 적용 시점**
   - Core User 사용 원칙은 3조도 1·2조와 동일하게 유지 (원칙 자체는 확정됨)
   - 다만 공동 편집 기능이 Nice to Have 단계라, **실제 적용 시점만 후순위**로 조정
   - 팀 정보 API는 3조도 우선 수신 대상에 포함
   - 우선순위: 낮음 (원칙 미확정이 아니라 일정상 후순위)
