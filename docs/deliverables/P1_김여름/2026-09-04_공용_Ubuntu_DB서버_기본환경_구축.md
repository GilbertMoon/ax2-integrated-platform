# 2026-09-04 공용 Ubuntu DB 서버 기본환경 구축

## 담당자
- P1 김여름
- 작업일: 2026-09-04
- 목적: 특정 개인 PC에 의존하지 않는 공용 DB 서버 기반 구축

## 오늘 완료 사항

### Ubuntu / Network
- Ubuntu 26.04.1 LTS 정상 부팅 확인
- GUI 환경 설치 및 재부팅 후 GUI 진입 확인
- Intel Wi-Fi 장치 및 `iwlwifi` 드라이버 확인
- NetworkManager 설치 및 Wi-Fi 인터페이스 관리 정상화
- Wi-Fi 연결 및 인터넷 통신 확인

### 사용자 환경
- Chrome 설치 및 웹 접속 확인
- ChatGPT 접속 확인
- Noto CJK 폰트 설치 상태 확인
- `ibus-hangul` 설치
- `Korean (Hangul)` Input Source 추가

### PostgreSQL
- PostgreSQL 18 설치 완료
- `main` Cluster `online` 확인
- Port 5432 확인
- `sudo -u postgres psql` 로컬 접속 성공
- `postgres=#` 프롬프트 확인

## 현재 판정
- Ubuntu 공용 서버 기본환경: GREEN
- PostgreSQL 설치/로컬 접속: GREEN
- 팀 공용 DB 외부 연동: PENDING

## 월요일 MUST
1. 기존 DB Schema/Data 상태 확인
2. 기존 DB 백업
3. 새 공용 서버 프로젝트 DB 생성
4. 프로젝트용 DB Role/User 및 권한 설정
5. 필요한 Schema/Data 이관
6. 공용 서버 IP 운영 기준 확정
7. PostgreSQL 외부 접속 설정
8. `pg_hba.conf` 접근 정책 설정
9. Firewall / Port 5432 확인
10. P1~P6 및 1·2·3조 원격 접속 Cross Check
11. Django `.env` DB HOST 전환
12. Migration / 조회 / 저장 Regression 검증
13. Backup / Restore 절차 구성
14. 서버 재부팅 후 PostgreSQL 자동 시작 재검증

## 운영 기준
월요일부터는 오늘 구축한 공용 Ubuntu 서버를 기준으로 DB 환경을 전환한다.

목적은 특정 개발자 PC의 전원·이동·네트워크 상태와 관계없이 1·2·3·4조의 소스 개발 및 통합 작업을 안정적으로 이어갈 수 있도록 하는 것이다.

외부 연동 및 Cross Check가 완료되기 전까지는 새 DB HOST를 임의로 코드에 반영하지 않는다.

## 보안 기준
GitHub 문서에는 다음 민감정보를 평문으로 기록하지 않는다.
- 실제 서버 IP
- MAC Address
- Wi-Fi SSID / Password
- Ubuntu 계정 Password
- DB Password
- `.env` 실제 값
- API Key / Token / Secret

필요한 값은 팀 내부에서 별도 안전한 경로로 공유한다.

## DONE 조건
- [x] 서버 OS / GUI / Network 기본환경 구축
- [x] PostgreSQL 설치 및 로컬 접속 검증
- [ ] 프로젝트 DB 전환
- [ ] 외부 접속 Cross Check
- [ ] Django 연결 및 Migration 검증
- [ ] Backup / Restore 검증

최종 공용 DB 서버 GREEN 판정은 월요일 외부 연동 및 프로젝트 연결 검증 완료 후 처리한다.
