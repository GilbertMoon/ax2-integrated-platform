# 2026-09-04 2조 LMS 최종 코드 확인 및 통합·회귀 테스트

## 1. 목적

* 2조 LMS 최종 전달 코드를 확인하여 최종 코드 및 버전 상태를 점검한다.

* 최신 `develop` 기준으로 로컬 작업 코드를 반영하고 전체 테스트를 수행하여 기존 기능의 Regression 여부를 확인한다.

* 2조 LMS의 최종 DB 구성, Migration, Slack 연동 및 환경변수 변경사항을 확인한다.

* 4조 Core 통합 전 LMS 실행 및 기능 상태를 확인하고, 통합 이후 추가 검증이 필요한 사항을 정리한다.

---

## 2. 오늘 수행 내용

* 2조 LMS 최종 코드 전달 상태 확인

* 2조 LMS Repository 및 `develop` 브랜치 확인

* 최종 전달 Commit `ee04c86` 확인

* 2조 최종 DB 구성 확인

* `assignment_lms` 및 `ax_evaluation` 2개 DB 구조 확인

* `accounts_client` read-only 연동 구조 확인

* 2조 Migration 현황 확인

* Slack 및 `SlackIdentity` 관련 최종 변경사항 확인

* 최신 `origin/develop` 상태 확인

* 기존 작업 브랜치에 최신 `develop` 변경사항 반영

* 최신 코드 기준 작업 트리 및 충돌 여부 확인

* 전체 pytest 실행

* 전체 테스트 결과 `203 passed, 87 warnings` 확인

* Django `check` 실행 및 정상 여부 확인

* `python manage.py runserver`를 통한 2조 LMS 서버 실행 확인

* Django 개발 서버 정상 실행 확인

* `/accounts/login/` 요청에 대한 Django 응답 확인

* Chrome을 통한 2조 LMS 수동 접속 테스트 진행

* 브라우저에서 LMS 화면이 정상적으로 표시되지 않고 무한 로딩되는 현상 확인

* 로컬 PostgreSQL `localhost:5432` 연결 상태 확인

* DB 접속 장애와 브라우저 접속 문제를 별도 사항으로 구분

* 튜터님 확인 후 현재 환경에서 추가적인 코드 수정 없이 작업 중단

---

## 3. 현재 코드 상태

### 3-1. 2조 최종 코드 기준

* Repository: `KANT-2/assignment-lms`

* Branch: `develop`

* 최종 전달 Commit: `ee04c8618be129acfbf83d069ea001b80514895d`

* Python: `3.14.6`

* Django: `6.1`

* PostgreSQL: `16.x`

### 3-2. DB 구성

* 기본 LMS DB: `assignment_lms`

* AX2 공용 DB: `ax_evaluation`

* 기본 LMS DB는 로컬 PostgreSQL을 사용하는 구조로 확인

* AX2 공용 DB는 별도 DB 서버를 사용하는 구조로 확인

* `accounts_client`는 `managed=False` 기반 read-only 구조로 확인

### 3-3. Migration 상태

* `apps/core`: `0001` ~ `0008_assignmentfile`

* `apps/tutor`: `0001_initial`, `0002_roundscore`

* `apps/github_sync`: `0001_initial`

* `apps/accounts_client`: 별도 Migration 없음

---

## 4. 자동 테스트 결과

### 4-1. 전체 pytest

* 전체 pytest 실행 → **PASS**

```text
203 passed, 87 warnings
```

* 테스트 실패 → **없음**

* Warning만 발생하였으며 전체 테스트는 정상 통과하였다.

### 4-2. Django System Check

* `python manage.py check` → **PASS**

```text
System check identified no issues (0 silenced).
```

* Django 설정 및 애플리케이션 기본 검사에서 오류가 발생하지 않았다.

---

## 5. 2조 LMS 서버 실행 및 수동 테스트

### 5-1. 서버 실행

* `python manage.py runserver` → **PASS**

* Django 개발 서버 정상 실행 확인

```text
Starting WSGI development server at http://127.0.0.1:8000/
```

* `/accounts/login/` 요청에 대해 Django 응답 확인

### 5-2. 브라우저 수동 테스트

* Chrome에서 2조 LMS 접속 → **FAIL / 확인 필요**

* Django 서버 자체는 정상 실행되었으나 Chrome에서 LMS 화면이 정상적으로 표시되지 않고 무한 로딩되는 현상 확인

* 해당 현상에 대해 튜터님 확인 진행

* 현재 환경에서는 추가적인 코드 수정 및 DB 변경 작업을 진행하지 않음

---

## 6. PostgreSQL 연결 확인

### 6-1. 로컬 DB 연결

현재 2조 `.env`의 기본 LMS DB는 로컬 PostgreSQL을 사용하도록 설정되어 있다.

```env
DB_NAME=assignment_lms
DB_HOST=localhost
DB_PORT=5432
```

로컬 PostgreSQL 포트 연결 확인:

```text
Test-NetConnection localhost -Port 5432

TcpTestSucceeded : True
```

* 로컬 PostgreSQL 서비스 연결 → **PASS**

### 6-2. 공용 DB 관련

* AX2 공용 `ax_evaluation` DB는 별도 서버 연결이 필요한 구조임을 확인

* 기존 공용 PostgreSQL 서버가 특정 팀원의 PC 및 내부 네트워크 환경에 의존하고 있어 팀원들의 DB 접근에 영향을 줄 수 있는 상황 확인

* 해당 내용은 금일 개발 작업과 별도의 DB 운영 장애 사항으로 분류함

---

## 7. Slack 및 통합 관련 확인

* 2조 최종 코드의 Slack 관련 설정 확인

* `SlackIdentity` 사용자 매핑 구조 확인

* AX User ID와 Slack Member ID 매핑 구조 확인

* LMS 제출 및 평가 관련 Slack 알림 구조 확인

* 4조 공통 Slack 모듈과의 중복 여부 확인

* 최종 통합 시 4조 공통 Slack 모듈을 기준으로 통합할 필요가 있음을 확인

---

## 8. 산출물 / 근거

* Issue: `[P5][전예진][2026-09-04] 2조 LMS 최종 코드 통합 및 Slack 회귀 테스트`

* Repository: `KANT-2/assignment-lms`

* Final Branch: `develop`

* Final Commit: `ee04c8618be129acfbf83d069ea001b80514895d`

* 전체 테스트 결과: `203 passed, 87 warnings`

* Django System Check: 정상

* 관련 이전 작업: `2026-09-03_2조_LMS_통합_회귀테스트.md`

* 오늘 산출물: `docs/deliverables/P5_전예진/2026-09-04_2조_LMS_최종_코드_통합_및_회귀테스트.md`

---

## 9. 검증 방법

* `git fetch origin`을 통한 2조 원격 저장소 최신 상태 확인

* 2조 `develop` 브랜치 및 최종 Commit 확인

* 최신 `develop` 변경사항 로컬 반영

* 작업 트리 및 충돌 여부 확인

* `pytest -q`를 통한 전체 테스트 실행

* `python manage.py check`를 통한 Django 기본 검사

* `python manage.py runserver`를 통한 개발 서버 실행

* `/accounts/login/` 요청 및 Django 응답 확인

* Chrome을 통한 실제 LMS 접속 테스트

* `Test-NetConnection localhost -Port 5432`를 통한 PostgreSQL 연결 확인

* 2조 DB 구성 및 `accounts_client` read-only 연동 구조 확인

* Slack 및 `SlackIdentity` 연동 구조 확인

---

## 10. 본인 검증

* 2조 최종 Repository 확인 → **PASS**

* 최종 `develop` 브랜치 확인 → **PASS**

* 최종 Commit `ee04c86` 확인 → **PASS**

* 2조 DB 구성 확인 → **PASS**

* Migration 상태 확인 → **PASS**

* 최신 `develop` 반영 → **PASS**

* 전체 pytest 실행 → **PASS**

* `203 passed, 87 warnings` 확인 → **PASS**

* Django System Check → **PASS**

* Django 개발 서버 실행 → **PASS**

* `/accounts/login/` Django 응답 확인 → **PASS**

* 로컬 PostgreSQL `localhost:5432` 연결 확인 → **PASS**

* Chrome 실제 LMS 접속 → **FAIL / 확인 필요**

---

## 11. Cross Check

* 2조 최종 코드 및 최종 Commit 기준 확인

* 2조 `assignment_lms` / `ax_evaluation` DB 구조 확인

* 2조 `SlackIdentity`와 4조 공통 Slack 모듈의 사용자 매핑 구조 확인

* 최신 `develop` 반영 후 전체 테스트 결과 확인

* Django 서버 실행 상태 확인

* 로컬 PostgreSQL 연결 상태 확인

* 브라우저 수동 접속 과정에서 발생한 무한 로딩 현상 확인

* DB 운영 장애와 LMS 브라우저 접속 문제를 별도 이슈로 구분

* 공용 DB 서버 이전 후 2조 LMS 및 4조 Core에 대한 추가 Cross Check가 필요함을 확인

---

## 12. 미해결 / 다음 작업

* 공용 PostgreSQL DB 서버 운영 구조 확정 필요

* 기존 DB 백업 및 복구 테스트 필요

* 공용 DB 서버 확정 후 2조 `.env` DB 설정 확인

* 4조 Core DB 연결 상태 확인

* 최소 2명 이상 DB 접속 Cross Check

* Migration 정상 적용 여부 재확인

* 2조 LMS 주요 기능 Regression 테스트

* Slack 제출 알림 및 제출 독려 기능 Regression 테스트

* 평가 완료 Slack 알림 Regression 테스트

* 4조 Core와 2조 LMS 통합 후 기존 기능 충돌 여부 확인

### 다음 작업

* 공용 DB 서버 이전 및 운영 기준 확인

* 공용 DB 연결 설정 통일

* 2조 LMS DB 연결 재확인

* 4조 Core DB 연결 재확인

* 2조 LMS 주요 기능 통합 Regression 테스트

* Slack 연동 Regression 테스트

* 4조 기존 기능 Regression 테스트

* 최종 통합 결과 및 테스트 결과 기록
