# [P2][심우섭][2026-09-02] 근태 DB 반영 · 팀 정보 API · 얼굴인식 연동 설계

- **상태:** Open
- **담당자:** P2 심우섭 (tladntjq1-lgtm)
- **역할:** 로그인 / 회원 / 학생 / 권한 / 근태 연계
- **선행 이슈:** #34 [P2][심우섭][2026-08-31] 로그인/회원/학생/권한/근태 실제 구현 시작
- **작성일:** 2026-09-02

## 목적

#34에서 근태 모듈(모델·API·운영 화면·권한)을 로컬(sqlite)에 구현 완료했다.
오늘은 이 결과를 **팀이 공유하는 상태로 올리고(커밋·PR·Postgres)**, #34에서 남은
**팀 정보 API(작업 2)** 와 **얼굴인식 연동(작업 4)** 을 이어서 진행한다.

---

## 작업 1. 근태 구현분 공유 상태로 올리기 (MUST)

- [ ] `feature/p2_wooseob` 에 오늘까지 변경분 커밋
  - 신규: `attendance/`, `templates/attendance/board.html`
  - 수정: `config/settings.py`, `config/urls.py`, `templates/includes/sidebar.html`, `docs/...`
- [ ] `feature/p2_wooseob` → `develop` PR 생성 (base = develop)
- [ ] 원본 개발 DB(Postgres `ax_evaluation`)에 `python manage.py migrate` 실행 → `attendance.0001_initial` 적용 — P3 협업
- [ ] `python manage.py showmigrations attendance` 로 `[X]` 확인
- [ ] 배포 서버 마이그레이션 항목을 MASTER_TODO PHASE 5에 연결
- [ ] 관련 이슈: `issue-attendance-postgres-migrate-2026-09-01.md`

## 작업 2. 근태 테스트 코드 (SHOULD)

- [ ] `attendance/tests.py` 작성
  - 튜터 GET/POST 정상, 학생 403, `/attendance/me/` 본인만 반환
  - `save_attendance_board` 상태 변경 / 빈 값 → 기록 삭제 / 유효하지 않은 user 무시
  - `update_attendance_view` 400/404 방어 경로

## 작업 3. 팀 정보 API 개발 (#34 작업 2 이어받기)

- [ ] 팀 정보 API 스펙 확정 (제공 필드: 팀 id, 팀 이름 등) — 1·2·3조와 필드 협의
- [ ] `GET /api/teams/me/` — 로그인한 사용자 본인 팀 조회 API
  - 참고: 현재 `teams` 앱에 `student_team_view`(`/teams/student-team`)가 있으나 경로·응답 스펙이 이슈와 다름 → 정리 필요
- [ ] `GET /api/teams/user/{user_id}/` — 특정 사용자 팀 조회 API (1조 팀 대결 매칭용)
- [ ] 권한: 근태와 동일하게 `is_operations_user` / `is_student_user` 재사용
- [ ] 1·2·3조에서 실제 연동 테스트

## 작업 4. 얼굴인식 연동 설계 (#34 작업 4 이어받기 — 설계만, 구현 보류)

- [ ] 얼굴인식 저장 방식 팀 논의: **원본 사진 vs 임베딩 벡터** — P3/P6 협업, 결정 필요
- [ ] 결정 후: 얼굴인식 결과 → 근태 자동 기록 경로 설계
  - 얼굴인식 성공 시 `AttendanceRecord` 를 `checked_by_face_recognition=True` 로 생성/갱신
  - 튜터가 화면에서 수동 저장하면 `False` 로 덮이는 규칙은 이미 구현됨
- [ ] 얼굴인식 결과 ↔ `accounts_user.id` 매칭 기준 문서화 (화면 표시는 이름, 내부는 id)

## 작업 5. 학생 본인 근태 조회 화면 필요 여부 확정 (COULD)

- [ ] 현재 학생용은 JSON API(`/attendance/me/`)만 존재
- [ ] 마이페이지 등에 학생 본인 근태 화면이 필요한지 튜터/팀과 확인 후 결정

---

## DONE 기준

- [ ] 근태 구현분이 `develop` 에 병합되고 Postgres에 마이그레이션 적용됨
- [ ] `attendance/tests.py` 가 통과함
- [ ] 팀 정보 API(`/api/teams/me/`, `/api/teams/user/{id}/`) 가 1·2·3조에 제공됨
- [ ] 얼굴인식 저장 방식이 팀 논의로 결정되고 연동 설계 문서가 존재함
- [ ] #34 의 해당 체크박스가 갱신됨

## 참고

- 얼굴인식 저장 방식 결정 전까지 작업 4는 **설계만** 진행, 구현은 보류 (#34 참고란과 동일)
- 관련 산출물: `docs/deliverables/P2_심우섭/2026-09-01_근태_UI_및_앱_통합.md`
- 관련 기준: `docs/deliverables/P2_심우섭/근태_얼굴인식_연결기준_20260827.md`
- 관련 TODO: `docs/01_MASTER_TODO.md` PHASE 4 / PHASE 5

## 진행 상황

| 시각 | 내용 |
|---|---|
| 이슈 생성 | P2 심우섭이 #34 후속으로 작성 |
