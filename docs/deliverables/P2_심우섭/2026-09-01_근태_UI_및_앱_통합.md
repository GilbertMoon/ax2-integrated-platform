# 2026-09-01 근태 모듈 앱 통합 및 운영 UI 추가

작성자: P2 심우섭 | 작성일: 2026-09-01

## 1. 목적

- `docs/deliverables/P2_심우섭/근태_얼굴인식_연결기준_20260827.md`에서 확정한 "근태 연결 기준"을
  실제 코드로 구현한다.
- 근태 데이터(`accounts_user.id` 기준)를 저장할 모델·API·운영 화면을 프로젝트에 통합한다.
- 기존 앱(`teams`, `rounds`)과 동일한 코딩 스타일·권한 체계를 그대로 따른다.

## 2. 오늘 수행 내용

### 2-1. `attendance` 앱 신규 생성

| 파일 | 내용 |
|---|---|
| `attendance/models.py` | `AttendanceRecord` 모델. `user`(FK→accounts_user) + `date` + `status`(present/late/absent) + `checked_by_face_recognition` + `memo`. `TextChoices` + `gettext_lazy` + `UniqueConstraint(user, date)` — `accounts/models.py` User 모델 컨벤션 준수 |
| `attendance/services.py` | 화면용 조회·저장 로직. `approved_students()`, `attendance_board_rows(day)`, `save_attendance_board(day, status_by_user)` |
| `attendance/views.py` | 운영 화면 뷰 `attendance_board`(GET/POST) + JSON API 3종(`my_attendance_view`, `user_attendance_view`, `update_attendance_view`) |
| `attendance/urls.py` | `app_name = "attendance"`, 4개 라우트 |
| `attendance/admin.py` | Django 관리자 등록. `OperationsAdminMixin`(튜터·관리자 조회/편집, 삭제는 관리자만) |
| `attendance/migrations/0001_initial.py` | `makemigrations`로 자동 생성 |
| `templates/attendance/board.html` | 근태 화면 템플릿. 기존 클래스(`card-custom`, `btn-ax-primary`, `ax-empty-state`) 재사용 |

### 2-2. 프로젝트 배선(2줄)

- `config/settings.py` `INSTALLED_APPS` 에 `"attendance.apps.AttendanceConfig"` 추가
- `config/urls.py` 에 `path("attendance/", include("attendance.urls"))` 추가

### 2-3. 사이드바 메뉴

- `templates/includes/sidebar.html` 운영 섹션에 "근태" 링크 1개 추가
  (`{% url 'attendance:board' %}`, 아이콘 `bi-calendar-check`)

### 2-4. 권한

- 새 권한 함수를 만들지 않고 `accounts/permissions.py`의 검증된 함수를 그대로 사용:
  - 운영 화면·`user`·`update` API: `is_operations_user`(튜터 또는 관리자, `is_active`·`approval_status` 검증 포함)
  - `me` API: `is_student_user` 또는 `is_operations_user`
- 학생은 운영 화면(`/attendance/`) 접근 시 403.

### 2-5. 부수 정리

- `update_attendance_view` 에 500 유발 지점 2곳 방어코드 추가
  (요청 본문이 dict가 아닌 경우, 날짜 문자열이 유효하지 않은 경우 → 각각 400 반환)
- 튜터가 화면에서 수동 저장하면 `checked_by_face_recognition`을 `False`로 덮음
  (얼굴인식 자동 기록보다 사람이 직접 넣은 값을 우선)

## 3. 결과

- 마이그레이션 생성·적용 완료 (로컬 sqlite):
  ```
  makemigrations attendance → 0001_initial (Create model AttendanceRecord)
  migrate attendance        → OK
  manage.py check           → 0 issues
  ```
- 제공 API / 화면
  | 경로 | 메서드 | 권한 | 기능 |
  |---|---|---|---|
  | `/attendance/` | GET/POST | 튜터·관리자 | 날짜별 수강생 출결 조회·입력 화면 |
  | `/attendance/me/` | GET | 학생·튜터 | 본인 근태 조회(JSON) |
  | `/attendance/user/<id>/` | GET | 튜터·관리자 | 특정 사용자 근태 조회(JSON) |
  | `/attendance/update/` | POST | 튜터·관리자 | 근태 1건 등록/수정(JSON) |
- 확정 기준(20260827 문서)과의 일치
  - 근태는 `accounts_user.id` 참조 ✔
  - `rounds_roundparticipant`와 무관 ✔
  - 조회/수정 권한은 튜터(+관리자)만, 학생은 본인 것만 ✔
  - 화면 표시는 id가 아니라 `first_name`(없으면 email) ✔ (`User.__str__`와 동일 패턴)

## 4. 산출물 / 근거

- Issue: `docs/deliverables/P2_심우섭/issue-attendance-postgres-migrate-2026-09-01.md`
  (원본 DB(Postgres) 반영 및 배포 서버 마이그레이션 요청)
- PR: (작성 예정 — `feature/p2_wooseob` → `develop`)
- Commit: (커밋 예정)
- 관련 문서:
  - `docs/deliverables/P2_심우섭/근태_얼굴인식_연결기준_20260827.md` (구현 기준)
  - `docs/01_MASTER_TODO.md` PHASE 4 "근태 모듈 설계/개발 — P2, P3 협업"

## 5. 검증 방법

1. 로컬 개발 데이터 준비: `python seed_data.py` (튜터 1 + 수강생 7 + 회차/팀 생성, idempotent)
2. `python manage.py runserver` 후 `http://localhost:8000/accounts/login/`
   - `tutor@ax.com` / `Local-Only-Strong-Password-2026!` 로 로그인
3. 왼쪽 사이드바 **근태** 클릭 → `/attendance/`
4. 상단 날짜를 **2026-09-01** 로 조회 → 수강생 7명 표가 나오고, 우측 상단에 출석/지각/결석/미기록 집계가 보임
5. 아무 학생의 상태를 바꾸고 **저장** → 상단에 "근태를 저장했습니다" 메시지, 새로고침해도 값 유지
6. 상태를 **미기록**으로 되돌리고 저장 → 그 행의 근태 기록이 삭제됨
7. 로그아웃 후 `student@ax.com` 로 로그인 → `/attendance/` 접근 시 **403**, `/attendance/me/` 는 본인 기록 JSON 정상

## 6. 본인 검증

- 결과: PASS
- 확인 내용:
  - `manage.py check` 0 issues
  - Django 테스트 클라이언트로 확인:
    - 튜터 `GET /attendance/?date=2026-09-01` → 200, 수강생 7행 렌더, 출석 집계 정확
    - 튜터 `POST /attendance/` → 302, 특정 학생 `absent` 반영, 다른 학생 빈 값 → 기록 삭제 확인
    - 학생 `GET /attendance/` → 403
    - 학생 `GET /attendance/me/` → 200, 본인 기록만 반환
    - 존재하지 않는 user_id로 저장 시도 → 조용히 무시(유효 수강생만 반영)

## 7. Cross Check

- 검증자: (미정 — P3 김종복 / 원 4조 담당자 협의 필요)
- 결과: 대기
- 확인 내용:
- 보완사항:

## 8. 미해결 / 다음 작업

- 남은 일:
  1. **원본 DB(Postgres)에 마이그레이션 반영** — 로컬 sqlite에만 적용된 상태.
     `.env`의 `POSTGRES_*` 활성화 후 `python manage.py migrate` 필요. (별도 Issue 참조)
  2. **배포 서버(Windows 11 Pro) 마이그레이션** — PHASE 5 항목.
  3. **얼굴인식 → 근태 자동 기록 연동** — 얼굴인식 결과가 `update_attendance_view`
     (또는 서비스 함수)를 호출해 `checked_by_face_recognition=True`로 자동 기록하는 경로.
     얼굴 데이터 저장 방식(원본 사진 vs 임베딩 벡터) 결정이 선행되어야 함 — P3/P6 협업.
  4. **학생 본인 근태 조회 화면** — 현재 학생용은 JSON API(`/attendance/me/`)만 있음.
     마이페이지 등에 화면이 필요한지 확정 필요.
  5. **`attendance/tests.py`** — 앱별 테스트 관례에 맞춰 테스트 코드 추가.
  6. 회차(Round)·기간 개념과의 연결 여부 확정 (현재 근태는 순수 날짜 기준, 회차와 무관).
- 이유: 얼굴인식 저장 방식 미정, 배포 환경 미구성(PHASE 5)
- 다음 담당자: P2 심우섭(계속) / P3 김종복(DB·마이그레이션) / 얼굴인식 주담당(연동)
- 다음 작업: 위 1·2 완료 후 3번 연동 설계 착수
